fn main() {
   let interest: f32 = 8;
   println!("Interest is {}", interest);
   /* 
correction quote due to inputing an integer in a float scalar data type

help: use a float literal
  |
2 |    let interest: f32 = 8.0;
  |                         ++

For more information about this error, try `rustc --explain E0308`.
error: could not compile `practice_6` (bin "practice_6") due to 1 previous error
*/
}
